TELEGRAM_BOT_TOKEN="8626032922:AAHf6CqafStmltAH_71YU4eKI0mB6TVZAYM"
# Тот же URL, что зарегистрирован в @BotFather как Web App URL
MINI_APP_URL="https://your-mini-app-domain.example"
# Та же база, что у backend — бот читает для уведомлений/рассылок; игровая
# логика (баланс, ставки, покупки) всегда идёт через backend, не напрямую в БД.
DATABASE_URL="postgresql://neo_merz:neo_merz@localhost:5432/neo_merz?schema=public"
# Как часто проверять, у кого "созрел" дневной бонус, в минутах
DAILY_BONUS_CHECK_INTERVAL_MINUTES=15
# Как часто проверять рассылки из админки (Этап 5), готовые к отправке, в минутах
BROADCAST_CHECK_INTERVAL_MINUTES=2

# Stars Payments (Этап 5, раздел 2.2 ТЗ) — межсервисный вызов bot -> backend
# при pre_checkout_query/successful_payment (см. src/payments.ts). Значение
# INTERNAL_API_SECRET должно СОВПАДАТЬ с тем, что задано в backend/.env.
BACKEND_INTERNAL_URL="http://localhost:3000/api/internal"
INTERNAL_API_SECRET="REPLACE_ME_WITH_A_LONG_RANDOM_SECRET"
